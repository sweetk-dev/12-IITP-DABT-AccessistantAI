# 보안 정책 (Security Policy)

본 저장소는 IITP 국책과제 「데이터 기반 장애인 데이터 탐색·활용 해결기술 개발」의 연구개발 산출물입니다.

## 취약점 신고

보안 취약점을 발견하셨다면 GitHub 의 비공개 취약점 신고 기능으로 제보해 주시기 바랍니다.

1. 본 저장소의 **Security** 탭으로 이동합니다.
2. **Report a vulnerability** 를 선택합니다.
3. 재현 절차와 영향 범위를 기재해 주십시오.

제보 내용은 저장소 관리자에게만 비공개로 전달되며, 확인 후 회신드립니다.

## 자격증명 취급 원칙

본 과제의 모든 저장소는 아래 원칙을 따릅니다.

- 데이터베이스 접속 정보, 외부 API 인증키, 계정 자격증명은 **저장소에 포함하지 않습니다.** 실행 환경의 환경변수로만 주입합니다.
- 커밋 반입 시점에 자격증명 패턴을 자동 검사하며, 검출 시 반입이 차단됩니다.
- 암호화된 값과 그 복호화 키를 같은 저장소에 함께 두지 않습니다.
- 압축 파일·데이터베이스 덤프 등 내용 검사가 불가능한 형식은 반입 전 해제하여 점검합니다.
- 분기 1회 전체 저장소를 대상으로 현재 파일, 커밋 이력, 게시물 본문에 대한 전수 점검을 수행합니다.

## 데이터 취급

본 과제는 장애인 관련 공공 데이터를 다룹니다. 저장소에 포함된 표본 데이터는
개인 식별 정보를 제거하거나 마스킹한 상태로만 관리합니다. 원본 데이터는 접근통제가 적용된
별도 환경에서만 취급합니다.

## 지원 범위

기본 브랜치(`main`)의 최신 릴리즈를 지원 대상으로 합니다.
